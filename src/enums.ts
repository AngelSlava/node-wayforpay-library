export enum regularMode {
  ONCE = 'once',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  QUARTERLY = 'quarterly',
  MONTHLY = 'monthly',
  HALFYEARLY = 'halfyearly',
  YEARLY = 'yearly'
}

export enum requestType {
  CREATE = 'CREATE',
  STATUS = 'STATUS',
  SUSPEND = 'SUSPEND',
  RESUME = 'RESUME',
  REMOVE = 'REMOVE',
  CHANGE = 'CHANGE',
}
